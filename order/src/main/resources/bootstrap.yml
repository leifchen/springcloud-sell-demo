spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      name: order
      profile: dev
eureka:
  client:
    service-url:
      defaultZone: http://localhost:28761/eureka/
  instance:
    prefer-ip-address: true
