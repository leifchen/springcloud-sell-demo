spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      name: user
      profile: dev
eureka:
  client:
    service-url:
      defaultZone: http://eureka1:28761/eureka/, http://eureka2:28762/eureka/
  instance:
    prefer-ip-address: true
